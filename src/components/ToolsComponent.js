import React from 'react'
import ResponsiveEmbed from 'react-responsive-embed'
export const Tools = () => {
return (

<CardImg width="100%" src={baseUrl + dish.image} alt={confusion} />

)
}

export default Tools;